# Domain layer

This layer will define the business concepts of the application: Brand, Product, SKU, Photo, extracted field provenance, Price, Job, Catalog and CatalogVersion.

The domain must not import a specific AI provider SDK or frontend code.
