# Grabelan catalog template

This directory will hold deterministic HTML/CSS catalog templates.

The renderer consumes an immutable CatalogVersion snapshot, never live product/price tables.

Brand assets should be added only when they are intentionally safe to publish in the repository.
